#include <stdio.h>
int n;
int sum = 0;

int main()
{
    printf("enter a number");
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
                 sum = sum + i;
        
    }
    printf("sum of the numbers %d",sum);
}
